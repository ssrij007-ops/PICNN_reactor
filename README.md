This approach is built for reacting flow based on the framework of Gao et al. (2021) ("Super-resolution and denoising of fluid flow using physics-informed convolutional neural networks without high-resolution labels,") which demonstrated that high-resolution flow fields can be recovered from sparse or low-resolution inputs without requiring any high-resolution training labels, by embedding the governing conservation laws and boundary conditions directly into the network's training loss.
